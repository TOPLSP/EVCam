<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.evcam">

    <!-- ========== 权限声明（按 API 级别分组） ========== -->
    
    <!-- 基础权限（所有版本） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.READ_LOGS"
        tools:ignore="ProtectedPermissions" />

    <!-- Android 6.0+ 权限 -->
    <uses-permission-sdk-23 android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Android 9.0+ 权限 -->
    <uses-permission-sdk-28 android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Android 10.0+ 权限（前台服务类型） -->
    <uses-permission-sdk-29 android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission-sdk-29 android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Android 11.0+ 权限 -->
    <uses-permission-sdk-30 android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Android 12.0+ 蓝牙权限 -->
    <uses-permission-sdk-31 android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Android 13.0+ 媒体权限 -->
    <uses-permission-sdk-33 android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission-sdk-33 android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- 声明使用摄像头功能 -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- ========== Application 配置 ========== -->
    <application
        android:name=".EVCamApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cam"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true"
        android:largeHeap="true"
        tools:targetApi="34">

        <!-- 高优先级保活 Provider -->
        <provider
            android:name=".KeepAliveProvider"
            android:authorities="${applicationId}.keepalive"
            android:exported="false"
            android:initOrder="2147483647" />

        <!-- FileProvider for sharing video files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ========== Activities ========== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PhotoViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.Cam.Fullscreen"
            android:screenOrientation="landscape" />

        <activity
            android:name=".VideoPlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.Cam.Fullscreen"
            android:screenOrientation="landscape" />

        <activity
            android:name=".TransparentBootActivity"
            android:exported="false"
            android:theme="@style/Theme.Cam.Transparent"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:screenOrientation="landscape" />

        <activity
            android:name=".LogcatViewerActivity"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:screenOrientation="landscape"
            android:exported="false" />

        <!-- ========== Services ========== -->
        
        <!-- 摄像头前台服务（Android 10+ 需要指定 foregroundServiceType） -->
        <service
            android:name=".CameraForegroundService"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <service
            android:name=".FloatingWindowService"
            android:exported="false" />

        <service
            android:name=".BlindSpotService"
            android:exported="false" />

        <!-- 无障碍服务 -->
        <service
            android:name=".KeepAliveAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- ========== Receivers ========== -->
        
        <!-- 开机自启动广播接收器 -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- 保活广播接收器 -->
        <receiver
            android:name=".KeepAliveReceiver"
            android:enabled="true"
            android:exported="true">
            
            <!-- 屏幕与电源相关 -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.SCREEN_ON" />
                <action android:name="android.intent.action.SCREEN_OFF" />
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
                <action android:name="android.intent.action.ACTION_POWER_DISCONNECTED" />
                <action android:name="android.intent.action.BATTERY_CHANGED" />
                <action android:name="android.intent.action.BATTERY_LOW" />
                <action android:name="android.intent.action.BATTERY_OKAY" />
            </intent-filter>

            <!-- 蓝牙相关 -->
            <intent-filter android:priority="1000">
                <action android:name="android.bluetooth.adapter.action.STATE_CHANGED" />
                <action android:name="android.bluetooth.adapter.action.CONNECTION_STATE_CHANGED" />
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
            </intent-filter>

            <!-- 存储与 USB 相关 -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.MEDIA_MOUNTED" />
                <action android:name="android.intent.action.MEDIA_UNMOUNTED" />
                <action android:name="android.intent.action.MEDIA_REMOVED" />
                <action android:name="android.intent.action.MEDIA_EJECT" />
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
                <action android:name="android.hardware.usb.action.USB_DEVICE_DETACHED" />
                <data android:scheme="file" />
            </intent-filter>

            <!-- 网络相关 -->
            <intent-filter android:priority="1000">
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.net.wifi.STATE_CHANGE" />
                <action android:name="android.net.wifi.SCAN_RESULTS" />
            </intent-filter>

            <!-- 音频与时间相关 -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.HEADSET_PLUG" />
                <action android:name="android.media.AUDIO_BECOMING_NOISY" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.DATE_CHANGED" />
            </intent-filter>

            <!-- 系统配置与应用相关 -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.LOCALE_CHANGED" />
                <action android:name="android.intent.action.AIRPLANE_MODE" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="com.kooo.evcam.ACTION_KEEP_ALIVE" />
            </intent-filter>

            <!-- 应用包相关 -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
